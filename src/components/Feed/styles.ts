import { GAP } from '@/app/constants';
import { css } from 'styled-components';

export const styles = {
  container: css`
    display: flex;
    justify-content: center;
    height: 100%;
    padding: ${GAP.M} 0;
  `
};
